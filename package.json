{
    "name": "hn",
    "version": "0.1.0",
    "scripts": {
        "fe": "bun --filter=hn.frontend",
        "be": "bun --filter=hn.backend",
        "fmt": "bunx --bun prettier -w .",
        "build": "bun --filter=hn.frontend run build",
        "preview": "bun --filter=hn.frontend run preview"
    },
    "workspaces": [
        "apps/*"
    ],
    "dependencies": {
        "@fontsource-variable/inter": "^5.2.6",
        "@uidotdev/usehooks": "^2.4.1"
    }
}
